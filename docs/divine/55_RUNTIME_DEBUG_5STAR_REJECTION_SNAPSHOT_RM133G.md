# RM1.33-G — 5★ Runtime Debug Rejection / Valid-Slot Snapshot Fixture v2

**Task ID:** RM1.33-G
**Status:** ✅ COMPLETE (read-only / inert)
**Baseline anchor:** `hero_skill_kit_catalog_baseline_rm132b_v4`
**Runtime attached:** `false`
**DB / Catalog writes:** none

---

## 1. Purpose

Mirror of RM1.33-F applied to the **5★ catalog side**: across all 20 native 5★ heroes, hit the existing debug preview endpoint on:

- **5 valid slots × 20 heroes = 100 valid cases** → each must return 200 with the inert/disabled runtime contract.
- **1 invalid slot (`ultimate`) × 20 heroes = 20 invalid cases** → each must return 404 with `reason=invalid_slot_for_5star`, no fallback to 6★ ultimate, no runtime payload, with the safety envelope still inert.

Cross-cutting safety: zero Marchio Boreale / Borea / Divine Weapon / Domain *application* in the 5★ payloads (declarative cap-policy invariants that name the Marchio system are allowed, as documented in §13).

---

## 2. Files

### Created
- `/app/data/design/hero_skill_kits/hero_skill_kit_runtime_debug_5star_snapshot_fixtures_v2.json` (~4.5 KB; compact template + 20 hero ids)
- `/app/backend/scripts/validate_runtime_debug_5star_snapshot_rejections.py`
- `/app/data/design/hero_skill_kits/hero_skill_kit_runtime_debug_5star_snapshot_result_v2.json` (machine-readable, regenerated by validator each run)
- `/app/docs/divine/55_RUNTIME_DEBUG_5STAR_REJECTION_SNAPSHOT_RM133G.md` (this file)

### Modified
- `/app/backend/scripts/run_hero_skill_kit_validator_suite.py` — added optional RM1.33-G entry (no required check weakened)

### Not modified
- 5★ / 6★ / Divine Weapon / Status / Borea catalogs (mtimes verified unchanged)
- Baseline v4 (mtime verified unchanged)
- Boss policy tables (RM1.34, RM1.34-B, RM1.34-C) — mtimes unchanged
- v1 snapshot fixture, v2 6★ snapshot fixture
- Runtime adapter + debug endpoints (still OFF / inert)
- `battle_engine.py`, `battle_core.py`, `combat.tsx`, HP bar runtime
- UI catalog screens
- DB / migrations / seed / gacha / roster
- Character Bible / assets

---

## 3. Fixture v2 Summary

- `fixture_id = hero_skill_kit_runtime_debug_5star_snapshot_fixtures_v2`
- `task_origin = RM1.33-G`
- 20 hero ids = exact match of the 5★ catalog (validator enforces equality vs `hero_skill_kits_5star_full_v1.json`)
- `valid_slots = [basic, passive_base, skill_1, passive_advanced, skill_2]`
- `invalid_slots = [ultimate]`
- `context_routing`:
  - `basic` → pvp · `passive_base` → boss · `skill_1` → pve · `passive_advanced` → boss · `skill_2` → pvp · `ultimate` → pvp
- Stored as **contract templates** (one valid-slot contract, one skill_2 contract, one invalid-ultimate contract) — the validator expands these into 100 valid + 20 invalid live cases. No 120 giant payload dumps.
- `forbidden_in_5star_payloads`: marchio_boreale, divine_weapon_runtime_layer, domain_runtime_layer, borea_activation, is_true_ultimate=true, ultimate_signature_upgrade
- `safety_flags`: `runtime_attached=false`, `design_only=true`, `no_borea_activation=true`, anchor `baseline_v4`

---

## 4. 5★ Valid Slot Coverage Results

**100/100 valid 5★ slot cases PASS**

Per case the validator asserts:
- `http_status = 200`
- `rarity = "5star"`
- `normalized_skill_slot.runtime_attached = false`, `battle_runtime_attached = false`
- `runtime_candidate.enabled = false`, `reason = "feature_flag_off"`, `payload = null`, `feature_flag = "SKILL_KIT_RUNTIME_ENABLED"`, `feature_flag_value = false`, `is_disabled_runtime_result = true`
- `cap_policy_preview.applied_to_combat = false`, `runtime_attached = false`, `preview_only = true`
- `safety_envelope.{debug_only, read_only}` = true; `safety_envelope.{runtime_enabled, applied_to_combat, db_write, catalog_write, roster_write, gacha_write, ui_runtime_control}` = false
- Forbidden 5★ leak check (see §7) PASS
- `borea_preview` absent (PASS)

Plus skill_2-specific asserts (20 heroes × skill_2):
- `normalized_skill_slot.skill_type ≠ "ultimate"` (PASS for all 20)
- `normalized_skill_slot.is_true_ultimate ≠ true` (PASS for all 20)

**Total per-field assertions on valid side:** ~2700 (≈27 per case × 100 cases). 0 failures.

---

## 5. 5★ Ultimate Rejection Results

**20/20 invalid `ultimate` cases PASS**

Per case the validator asserts (against the HTTP 404 detail payload):
- `http_status = 404`
- `detail.error = "invalid_slot"`
- `detail.reason = "invalid_slot_for_5star"`
- `detail.rarity = "5star"`
- `detail.fallback_disabled = true`
- No `runtime_candidate` in detail
- No `normalized_skill_slot` in detail
- `detail.safety_envelope.{debug_only, read_only}` = true; `{runtime_enabled, applied_to_combat, db_write, catalog_write, roster_write, gacha_write}` = false
- Forbidden 5★ leak check in detail PASS

**Total per-field assertions on invalid side:** ~280 (≈14 per case × 20 cases). 0 failures.

Coverage endpoint confirms `5star_ultimate_safely_rejected_count = 20` (cross-verifies the live count).

---

## 6. Runtime Disabled Contract Results

Across all 120 cases (100 valid + 20 invalid):
- `SKILL_KIT_RUNTIME_ENABLED` reported as `false` in every safety envelope and runtime_candidate
- `runtime_attached`, `battle_runtime_attached`, `applied_to_combat`, `db_write`, `catalog_write`, `roster_write`, `gacha_write`, `ui_runtime_control` all `false` in every payload
- No `runtime_candidate.payload` ever non-null
- Coverage endpoint snapshot: `runtime_enabled=false`, `marchio_boreale_borea_only=true`, `borea_catalog_only=true`, `5star_ultimate_safely_rejected_count=20`

---

## 7. Borea / Marchio / DW / Domain Safety

Semantic leak checks across all 5★ responses:

| Leak class | Method | Result |
|---|---|---|
| Marchio status applied to 5★ hero | `normalized_skill_slot.core_status_ids` must NOT contain `marchio_boreale` | 100/100 + 20/20 PASS — **0 leaks** |
| Marchio effect tag on 5★ hero | `normalized_skill_slot.core_effect_tags` must NOT contain `marchio_boreale` | 100/100 + 20/20 PASS — **0 leaks** |
| Borea hero swap | hero_id field anywhere must NOT be `greek_borea` (when not requested) | 100/100 + 20/20 PASS — **0 leaks** |
| DW / Domain runtime live | `divine_weapon_runtime_active`, `domain_runtime_active`, `live_numeric_modifier_applied`, `is_true_ultimate` = true forbidden | 100/100 + 20/20 PASS — **0 leaks** |
| `ultimate_signature_upgrade` effect tag on 5★ | not in `core_effect_tags` | 100/100 + 20/20 PASS — **0 leaks** |
| `borea_preview` block in 5★ response | response.borea_preview must be empty/absent | 100/100 + 20/20 PASS — **0 leaks** |

**Acknowledged allowed pattern:** the response's `cap_policy_preview` includes **declarative invariant fields** like `marchio_boreale_max_stacks=3` and `marchio_owner_hero_id="greek_borea"`. These are read-only system invariants exposed by the cap policy preview (the same value for every hero), not Marchio/Borea *application* to the 5★. They are deliberately **not** treated as leaks. This decision is documented here and inside the validator (§ FORBIDDEN_STATUS_IDS_IN_5STAR / has_forbidden_in_5star).

---

## 8. Validator Results

`python3 /app/backend/scripts/validate_runtime_debug_5star_snapshot_rejections.py` → **PASS**
- Fixture hero-id set vs catalog: 20/20 exact match
- 100 valid slot cases PASS (≈2700 assertions)
- 20 invalid ultimate cases PASS (≈280 assertions)
- Coverage endpoint cross-check PASS (`5star_ultimate_safely_rejected_count=20`)
- `/api/heroes` count=100, Borea hidden, legacy aliases 404 — all confirmed via the validator's own HTTP probes
- Result JSON written to `/app/data/design/hero_skill_kits/hero_skill_kit_runtime_debug_5star_snapshot_result_v2.json`

---

## 9. Suite / Baseline Results

`run_hero_skill_kit_validator_suite.py --include-baseline-diff` → **PASS 27/27** (0 fail, 0 miss)
- 13 required + 13 optional + 1 baseline diff = 27 checks, all green
- Baseline diff clean under `hero_skill_kit_catalog_baseline_rm132b_v4` **without** `--allow-changed`
- Invariants confirmed: 5★=20, 6★=13, DW=13, Marchio leak in non-Borea = 0, forbidden hero IDs = 0

---

## 10. API Smoke

| Endpoint | Result |
|---|---|
| `GET /api/health` | 200 |
| `GET /api/heroes` | 200 — **count = 100** |
| `GET /api/hero-skill-kits/catalogs/summary` / `5star` | 200 |
| `GET /api/hero-skill-kits/catalogs/by-hero/greek_atalanta` | 200 |
| `GET /api/hero-skill-kits/catalogs/by-hero/norse_eir` | 200 |
| `GET /api/hero-skill-kits/catalogs/by-hero/greek_borea` | 200 (catalog-only) |
| `GET /api/hero-skill-kits/catalogs/by-hero/borea` | **404** ✓ |
| `GET /api/hero-skill-kits/catalogs/by-hero/primordial_gaia` | **404** ✓ |
| `GET /api/divine-weapons/catalogs/summary` | 200 |
| `GET /api/hero-skill-kits/runtime/debug/coverage` | 200 (`5star_ultimate_safely_rejected_count=20`) |
| `GET /api/hero-skill-kits/runtime/debug/preview?hero_id=greek_atalanta&slot=skill_2&context=pvp` | 200 |
| `GET /api/hero-skill-kits/runtime/debug/preview?hero_id=norse_eir&slot=skill_2&context=pve` | 200 |
| `GET /api/hero-skill-kits/runtime/debug/preview?hero_id=celtic_mist_banshee&slot=passive_advanced&context=boss` | 200 |
| `GET /api/hero-skill-kits/runtime/debug/preview?hero_id=greek_atalanta&slot=ultimate&context=pvp` | **404** ✓ (invalid_slot_for_5star) |

No new API route was created.

---

## 11. UI Safety

- `hero-skill-kits-catalog.tsx`, `divine-weapons-catalog.tsx`: no POST/PUT/PATCH/DELETE; no runtime action buttons.
- Frontend grep: zero references to `5star_snapshot`, `snapshot_fixtures_v2`, `runtime/debug/preview`, `SKILL_KIT_RUNTIME_ENABLED`, `invalid_slot_for_5star`.
- No new UI screens / Pressables.
- Expo dev server RUNNING; localhost:3000 → 200.

---

## 12. `/api/heroes` Safety

`count = 100` (invariant respected). `borea` / `greek_borea` / `primordial_gaia` all hidden ✓.

---

## 13. Runtime / DB / Gacha / Roster / Catalog Safety

| Surface | Status |
|---|---|
| `SKILL_KIT_RUNTIME_ENABLED` | remains `false` |
| Runtime adapter | OFF / inert (`adapter imported by runtime = False`) |
| Battle runtime / `battle_engine.py` / `battle_core.py` | unmodified |
| Combat UI / `combat.tsx` | unmodified |
| Debug endpoints | unchanged, still inert |
| DB writes | none |
| 5★ catalog (`hero_skill_kits_5star_full_v1.json`) | unmodified (mtime 20:36 unchanged) |
| 6★ catalog / DW catalog / Status catalog / Borea catalog | unmodified |
| Baseline v4 | unmodified (mtime 21:03 unchanged) |
| Boss policy tables (RM1.34/RM1.34-B/RM1.34-C) | unmodified |
| v1 snapshot fixture / v2 6★ snapshot fixture | unmodified |
| Gacha / Roster | unmodified |
| Borea visibility | unchanged (catalog-only, hidden from `/api/heroes`) |
| New API routes / UI buttons | none |

The only files written by this task are the four listed in section 2 (fixture v2, validator, result JSON, this checkpoint doc) plus the optional suite entry in `run_hero_skill_kit_validator_suite.py`.

---

## 14. Warnings / Discrepancies

- Validator dedicated run: **0 warnings, 0 failures** across ~3000 per-field assertions on 120 cases.
- Documented decision: declarative cap-policy invariant fields naming the Marchio system (`marchio_boreale_max_stacks`, `marchio_owner_hero_id="greek_borea"`) are NOT treated as leaks. The semantic leak check focuses on Marchio/Borea **application** to the 5★ hero (status_ids, effect_tags, hero_id swap, runtime-live flags). This mirrors the same pragmatic distinction adopted in RM1.33-F.
- `is_true_ultimate` is not exposed as a literal field by the preview payload — the equivalent contract is verified via `normalized_skill_slot.skill_type ≠ "ultimate"` for 5★ skill_2 + global coverage invariants (matches the prompt's "OR equivalent field" clause).
- `audit_balance_foundation_boss_pvp_caps.py` continues to report the pre-existing **2 domain_stack_policy** + **1 dw_future_cap** informational WARNs (aligned with RM1.32-C delta plan). **No new warnings** introduced by RM1.33-G.

---

## 15. Final Recommendation

✅ **RM1.33-G is accepted.** All 26 acceptance criteria from the prompt are met:

1. Fixture v2 JSON created.
2. Validator created.
3. Result JSON created.
4. Checkpoint doc created.
5. 20/20 5★ heroes covered.
6. 100/100 valid 5★ slots return 200 with valid response.
7. 100/100 runtime candidates disabled.
8. 100/100 `reason = feature_flag_off`.
9. 20/20 5★ ultimate requests rejected safely (404 invalid_slot_for_5star).
10. 20/20 no fallback / payload for invalid ultimate.
11. 20/20 skill_2 not true ultimate (via skill_type and is_true_ultimate checks).
12. **0 Marchio Boreale application** in 5★ status_ids / effect_tags.
13. **0 Borea hero-id swap** in 5★ payloads.
14. **0 Divine Weapon / Domain runtime-live fields** in 5★.
15. No catalog data modified.
16. No baseline modified.
17. No boss policy tables modified.
18. No runtime / DB / gacha / roster changes.
19. Suite PASS (27/27).
20. Baseline diff PASS under v4.
21. API smoke PASS.
22. UI safety PASS.
23. `/api/heroes` remains 100.
24. Borea hidden.
25. Runtime adapter remains OFF / inert.
26. Docs report final status.

The 5★ side is now contractually mirrored to the 6★ side (RM1.33-F): together they form a **repeatable HTTP-level safety net** for the debug preview endpoint.

---

## 16. Suggested Next Tasks

- 🟢 **P3 — RM1.32-C2 (opt)**: Trim numeric foundation drafts ahead of baseline v5.
- 🟡 **P2 — RM1.33-H (future, hypothetical)**: Divine Weapon preview rejection / catalog-only safety fixture (mirror this pattern on the DW side; verify that DW catalog endpoints expose only the 13 DW kits and reject any runtime-equip request).
- 🟡 **P2 — RM1.34-E (future, hypothetical)**: Boss-side test fixture seed with 1–2 example bosses per family, exercising the 3 boss policy tables in a static scenario JSON.
- 🟡 **P2 (future)**: Collection Synergies V2 Activation.
- 🟡 **P2 (future)**: Affinity System Phase 2 — Gift catalog driven by Faction × Element matrix.
