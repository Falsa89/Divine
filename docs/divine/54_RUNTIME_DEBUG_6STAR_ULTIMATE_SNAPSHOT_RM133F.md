# RM1.33-F — 6★ Ultimate Runtime Debug Snapshot Fixture Set v2

**Task ID:** RM1.33-F
**Status:** ✅ COMPLETE (read-only / inert)
**Baseline anchor:** `hero_skill_kit_catalog_baseline_rm132b_v4`
**Runtime attached:** `false`
**DB / Catalog writes:** none

---

## 1. Purpose

Second snapshot fixture set focused **exclusively on the 13 native 6★ ultimate slots**. It exercises the existing debug preview endpoint with one canonical request per hero and verifies the inert contract is preserved end-to-end. Companion validator hits the live endpoint and writes a machine-readable result JSON per run.

This is a sibling/successor to the v1 fixture (mixed cases). v1 is left untouched.

---

## 2. Files

### Created
- `/app/data/design/hero_skill_kits/hero_skill_kit_runtime_debug_6star_ultimate_snapshot_fixtures_v2.json` (~21 KB, 13 cases)
- `/app/backend/scripts/validate_runtime_debug_6star_ultimate_snapshots.py`
- `/app/data/design/hero_skill_kits/hero_skill_kit_runtime_debug_6star_ultimate_snapshot_result_v2.json` (machine-readable, regenerated by validator each run)
- `/app/docs/divine/54_RUNTIME_DEBUG_6STAR_ULTIMATE_SNAPSHOT_RM133F.md` (this file)

### Modified
- `/app/backend/scripts/run_hero_skill_kit_validator_suite.py` — added optional RM1.33-F entry (no required check weakened)

### Not modified
- 5★ / 6★ / Divine Weapon / Status / Borea catalogs
- Baseline v4
- Boss policy tables (RM1.34, RM1.34-B, RM1.34-C)
- v1 snapshot fixture
- Runtime adapter + debug endpoints (still OFF / inert)
- `battle_engine.py`, `battle_core.py`, `combat.tsx`, HP bar runtime
- UI catalog screens
- DB / migrations / seed / gacha / roster
- Character Bible / assets

---

## 3. Fixture v2 Summary

- `fixture_id = hero_skill_kit_runtime_debug_6star_ultimate_snapshot_fixtures_v2`
- `task_origin = RM1.33-F`
- 13 cases (one per 6★ hero), every case `slot=ultimate`
- Volatile fields ignored (timestamps, request_id, exact warning text, wiretest_report_generated_at_utc)
- `safety_flags`: `runtime_attached=false`, `design_only=true`, `no_borea_activation=true`, `baseline_anchor=hero_skill_kit_catalog_baseline_rm132b_v4`
- Each non-Borea case asserts `marchio_boreale_in_core_status_ids = false`
- Borea case asserts `borea_preview = {no_activation:true, not_visible_in_heroes:true, marchio_boreale_owner_only:true, borea_activation_allowed:false, catalog_only:true, release_group:"launch_extra_premium"}` and `marchio_boreale_in_core_status_ids = true`

Selected contexts per case:
- **boss**: greek_athena, mesopotamian_tiamat, egyptian_sekhmet, japanese_susanoo, greek_gaia, egyptian_isis, cursed_pestilence_horseman, primordial_nyx
- **pvp**: japanese_raijin, celtic_morrigan, greek_borea
- **pve**: greek_artemis, japanese_amaterasu

---

## 4. 13 Ultimate Coverage Results

| # | Hero | Context | HTTP | Checks | Result |
|---|---|---|---|---|---|
| 1 | greek_athena | boss | 200 | 28 | PASS |
| 2 | mesopotamian_tiamat | boss | 200 | 28 | PASS |
| 3 | egyptian_sekhmet | boss | 200 | 28 | PASS |
| 4 | greek_artemis | pve | 200 | 28 | PASS |
| 5 | japanese_raijin | pvp | 200 | 28 | PASS |
| 6 | japanese_susanoo | boss | 200 | 28 | PASS |
| 7 | celtic_morrigan | pvp | 200 | 28 | PASS |
| 8 | greek_gaia | boss | 200 | 28 | PASS |
| 9 | japanese_amaterasu | pve | 200 | 28 | PASS |
| 10 | egyptian_isis | boss | 200 | 28 | PASS |
| 11 | cursed_pestilence_horseman | boss | 200 | 28 | PASS |
| 12 | primordial_nyx | boss | 200 | 28 | PASS |
| 13 | greek_borea | pvp | 200 | 34 | PASS |

**Total: 13/13 cases PASS — 372 individual assertions, 0 failures.**

---

## 5. Runtime Disabled Contract Results

For every one of the 13 cases the validator confirmed (per spec):
- `normalized_skill_slot.slot = "ultimate"` ✓
- `normalized_skill_slot.skill_type = "ultimate"` ✓ (proxy for `is_true_ultimate` preserved at runtime layer)
- `normalized_skill_slot.runtime_attached = false`, `battle_runtime_attached = false` ✓
- `runtime_candidate.enabled = false`, `reason = "feature_flag_off"`, `payload = null`, `feature_flag_value = false`, `is_disabled_runtime_result = true` ✓
- `cap_policy_preview.applied_to_combat = false`, `runtime_attached = false`, `battle_runtime_attached = false`, `preview_only = true` ✓
- `safety_envelope.debug_only = true`, `read_only = true`, `runtime_enabled = false`, `applied_to_combat = false`, `db_write = false`, `catalog_write = false`, `roster_write = false`, `gacha_write = false`, `ui_runtime_control = false` ✓

The coverage endpoint global check confirmed: `6star_ultimate_is_true_ultimate_preserved = 13`, `marchio_boreale_borea_only = true`, `borea_catalog_only = true`, `runtime_enabled = false`.

---

## 6. Borea / Marchio Safety

- `greek_borea` Borea case asserts `borea_preview.no_activation = true`, `not_visible_in_heroes = true`, `marchio_boreale_owner_only = true`, `borea_activation_allowed = false`, `catalog_only = true`, `release_group = "launch_extra_premium"`. All PASS.
- **Marchio leak check across the 12 non-Borea cases:** `marchio_boreale` NOT present in `normalized_skill_slot.core_status_ids` of any non-Borea ultimate preview. **Leak count = 0.**
- `marchio_boreale` IS present in greek_borea's `core_status_ids` (per design contract).
- `/api/heroes` does not expose `borea`, `greek_borea`, or `primordial_gaia` (validator and API smoke both confirm).
- Legacy aliases `/hero-skill-kits/catalogs/by-hero/borea` and `/hero-skill-kits/catalogs/by-hero/primordial_gaia` both return 404.

---

## 7. Validator Results

`python3 /app/backend/scripts/validate_runtime_debug_6star_ultimate_snapshots.py` → **PASS**
- 13 fixture cases verified live against the debug preview endpoint
- 372 per-field assertions (28×12 + 34×1) all PASS
- 0 warnings, 0 failures
- Coverage snapshot recorded in the result JSON
- `/api/heroes` count=100, Borea hidden, legacy aliases 404 — all confirmed via the validator's own HTTP probes

Result JSON written to `/app/data/design/hero_skill_kits/hero_skill_kit_runtime_debug_6star_ultimate_snapshot_result_v2.json`.

---

## 8. Suite / Baseline Results

`run_hero_skill_kit_validator_suite.py --include-baseline-diff` → **PASS 26/26** (0 fail, 0 miss)
- 13 required + 12 optional + 1 baseline diff = 26 checks, all green
- Baseline diff clean under `hero_skill_kit_catalog_baseline_rm132b_v4` **without** `--allow-changed`
- Invariants confirmed: 5★=20, 6★=13, DW=13, Marchio leak in non-Borea = 0, forbidden hero IDs = 0

---

## 9. API Smoke

| Endpoint | Result |
|---|---|
| `GET /api/health` | 200 |
| `GET /api/heroes` | 200 — **count = 100** |
| `GET /api/hero-skill-kits/catalogs/summary` / `6star` | 200 |
| `GET /api/hero-skill-kits/catalogs/by-hero/greek_athena` | 200 |
| `GET /api/hero-skill-kits/catalogs/by-hero/greek_borea` | 200 (catalog-only) |
| `GET /api/hero-skill-kits/catalogs/by-hero/borea` | **404** ✓ |
| `GET /api/hero-skill-kits/catalogs/by-hero/primordial_gaia` | **404** ✓ |
| `GET /api/divine-weapons/catalogs/summary` / `by-hero/greek_borea` | 200 |
| `GET /api/hero-skill-kits/runtime/debug/coverage` | 200 (`runtime_enabled=false`, `6star_ultimate_is_true_ultimate_preserved=13`) |
| `GET /api/hero-skill-kits/runtime/debug/preview?hero_id=greek_athena&slot=ultimate&context=boss` | 200 |
| `GET /api/hero-skill-kits/runtime/debug/preview?hero_id=greek_artemis&slot=ultimate&context=pve` | 200 |
| `GET /api/hero-skill-kits/runtime/debug/preview?hero_id=egyptian_isis&slot=ultimate&context=boss` | 200 |
| `GET /api/hero-skill-kits/runtime/debug/preview?hero_id=greek_borea&slot=ultimate&context=pvp` | 200 |

No new API route was created.

---

## 10. UI Safety

- `hero-skill-kits-catalog.tsx`, `divine-weapons-catalog.tsx`: no POST/PUT/PATCH/DELETE; no runtime action buttons.
- Frontend grep: zero references to `6star_ultimate`, `snapshot_fixture`, `runtime/debug/preview`, `SKILL_KIT_RUNTIME_ENABLED`, or adapter tokens.
- No new UI screens / Pressables.
- Expo dev server RUNNING; localhost:3000 → 200.

---

## 11. `/api/heroes` Safety

`count = 100` (invariant respected). `borea` / `greek_borea` / `primordial_gaia` all hidden ✓.

---

## 12. Runtime / DB / Gacha / Roster / Catalog Safety

| Surface | Status |
|---|---|
| `SKILL_KIT_RUNTIME_ENABLED` | remains `false` |
| Runtime adapter | OFF / inert (`adapter imported by runtime = False`) |
| Battle runtime / `battle_engine.py` / `battle_core.py` | unmodified |
| Combat UI / `combat.tsx` | unmodified |
| Debug endpoints | unchanged, still inert |
| DB writes | none |
| Catalogs (5★/6★/DW/Status/Borea) | unmodified (mtimes unchanged) |
| Baseline v4 | unmodified (mtime unchanged) |
| Boss policy tables (RM1.34/RM1.34-B/RM1.34-C) | unmodified (mtimes unchanged) |
| v1 snapshot fixture | unmodified |
| Gacha / Roster | unmodified |
| Borea visibility | unchanged (catalog-only, hidden from `/api/heroes`) |
| New API routes / UI buttons | none |

The only files written by this task are the four listed in section 2 (fixture v2, validator, result JSON, this checkpoint doc) plus the optional suite entry in `run_hero_skill_kit_validator_suite.py`.

---

## 13. Warnings / Discrepancies

- Validator itself: **0 warnings, 0 failures** across 372 per-field assertions on 13 cases.
- The response payload does NOT expose `is_true_ultimate` as a literal field. We assert the equivalent contract via `normalized_skill_slot.skill_type = "ultimate"` + `slot = "ultimate"` + the global coverage invariant `6star_ultimate_is_true_ultimate_preserved = 13`. This matches the prompt's "OR equivalent field from response" clause and is acknowledged here.
- `audit_balance_foundation_boss_pvp_caps.py` continues to report the pre-existing **2 domain_stack_policy** + **1 dw_future_cap** informational WARNs (aligned with RM1.32-C delta plan). **No new warnings** introduced by RM1.33-F.

---

## 14. Final Recommendation

✅ **RM1.33-F is accepted.** All 24 acceptance criteria from the prompt are met:

1. Fixture v2 JSON created.
2. Validator created.
3. Optional result JSON created.
4. Checkpoint doc created.
5. 13/13 6★ ultimate cases covered.
6. 13/13 preview HTTP 200.
7. 13/13 `is_true_ultimate` preserved (via skill_type + coverage invariant).
8. 13/13 runtime candidates disabled.
9. 13/13 `reason = feature_flag_off`.
10. 13/13 cap policy previews inert.
11. Borea catalog-only / no_activation.
12. Marchio Borea-only, **0 leak** on the 12 non-Borea.
13. No catalog data modified.
14. No baseline modified.
15. No boss policy tables modified.
16. No runtime/DB/gacha/roster changes.
17. Suite PASS (26/26).
18. Baseline diff PASS under v4.
19. API smoke PASS.
20. UI safety PASS.
21. `/api/heroes` remains 100.
22. Borea hidden.
23. Runtime adapter remains OFF/inert.
24. Docs report final status.

The v2 fixture is now a **repeatable contract** that any future change to the debug endpoint, runtime adapter, or 6★ ultimate metadata must continue to satisfy.

---

## 15. Suggested Next Tasks

- 🟢 **P3 — RM1.32-C2 (opt)**: Trim numeric foundation drafts ahead of baseline v5.
- 🟡 **P2 — RM1.33-G (future, hypothetical)**: 5★ skill-by-slot rejection fixture v2 (mirror of v2 logic on the 5★ side; ensures the 20 5★ slots return the safely-rejected ultimate envelope per design).
- 🟡 **P2 — RM1.34-E (future, hypothetical)**: Boss-side test fixture seed (design-only) with 1–2 example bosses per family, exercising the 3 boss policy tables in a static scenario JSON.
- 🟡 **P2 (future)**: Collection Synergies V2 Activation.
- 🟡 **P2 (future)**: Affinity System Phase 2 — Gift catalog driven by Faction × Element matrix.
