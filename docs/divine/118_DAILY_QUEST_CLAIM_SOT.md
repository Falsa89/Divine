# Pack 98 — Daily Quest Claim SOT

Source: `daily_quest_completion_claim`. claim_key: `daily_quest_<server_id>_<quest_id>_<YYYY-MM-DD UTC>`.
Quest whitelist: `daily_quest_1/2/3`. Fixed reward `{mission_coins: 15, honor: 8}`. Per-source kill switch `DAILY_QUEST_CLAIM_ENABLED` default OFF.

**Completion proof required for real users** → `DAILY_QUEST_COMPLETION_REQUIRED` blocker. Test bypass `test_completion_proof=true` allowed solo con marker `pack_98_test_artifact=true` → altrimenti 403 `TEST_COMPLETION_PROOF_FORBIDDEN_FOR_NON_TEST_USER`.

Status: **`READY_GATED_COMPLETION_REQUIRED`**. Pack 98 NON sblocca grant a player reali.
