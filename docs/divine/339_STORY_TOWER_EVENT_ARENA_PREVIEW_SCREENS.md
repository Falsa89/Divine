# 339 — Story/Tower/Event/Arena Preview Screens

Pack: `MEGA_RELEASE_ACCELERATION_7_MULTI_MODE_VISUAL_PREVIEW_SHELL_BATCH_PACK_v58`
Track: B
Tag: `PUBLIC_SYNC_TAG_v58_MEGA_RELEASE_ACCELERATION_7_MULTI_MODE_VISUAL_PREVIEW_SHELL_BATCH`

Quattro nuove schermate deeplink-only:
- `/story-visual-preview` (seed `story-alpha-v58`)
- `/tower-visual-preview` (seed `tower-alpha-v58`)
- `/event-visual-preview` (seed `event-alpha-v58`)
- `/arena-visual-preview` (seed `arena-alpha-v58`)

Caratteristiche comuni: deeplink-only, static, no backend, no battle_engine, no claim, no reward, no Reanimated, no import da `story.tsx` o `combat.tsx`, italiano, default fallback per ogni mode-specific field.
